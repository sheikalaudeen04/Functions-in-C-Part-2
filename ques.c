//wap to count no. od digitd using recursive fn
//wap to print the elements in an array using recursive fn
//wap to check the given string is palindrome or not by using recursive fn
//gcd
/*int gcd(int a,int b){
if(b==0)
return a;
 else
return gcd(b,a%b);
}
int main(){
printf("%d",gcd(10,20);
}*/
//wap to find the lcm of 2 no. using recursion.
//wap to find the nth term in the fibnocci sequence using recursive fn.
/*
#include<stdio.h>
int fibo(int n){
if(n==0)
return 0;
else if(n==1)
return 1;
else
return fibo(n-1)+fibo(n-2);
}
int main(){
printf("%d",fibo(6));
}
